# Decision drafts

Holding pen. Nothing here has been reviewed.

Written by the `/log` skill and the `SessionEnd` hook. Reviewed and promoted to
`docs/DECISIONS.md` by `/log-review`. Entries are appended newest at the
bottom; promotion reverses that, newest first.

`INFERRED` on a line means it was reconstructed, not stated. Correct it or cut
it — it must never reach `docs/DECISIONS.md` carrying that marker.

<!-- Drafts go below this line, newest at the bottom. -->
